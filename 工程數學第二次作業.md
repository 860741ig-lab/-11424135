We are asked to find the Fourier series of

$f(x)=x+π,x∈[−π,π].$

The standard Fourier series form is:

$f(x)\sim \frac{a_0}{2}+\sum_{n=1}^{\infty}\left(a_ncos nx+b_n sin nx\right)$

step 1: Compute $a_0$
​

$a_0=\frac{1}{\pi}\int_{-\pi}^{\pi}(x+\pi)\,dx$

Split the integral:

$\int_{-\pi}^{\pi} x\,dx = 0 \quad (\text{odd function})$

$\int_{-\pi}^{\pi} \pi\,dx = 2\pi^2$

so,

$a_0=\frac{1}{\pi}(2\pi^2)=2\pi\quad \Rightarrow \quad \frac{a_0}{2}=\pi$

step 2: Compute $a_n$
	​

$a_n=\frac{1}{\pi}\int_{-\pi}^{\pi}(x+\pi)\cos(nx)\,dx$

Split the integral:

x cos nx is an odd function → integral is 0

π cos nx integrates to 0 over [−π,π]

Thus,

$a_n=0$

step 3: Compute $b_n$

$b_n=\frac{1}{\pi}\int_{-\pi}^{\pi}(x+\pi)\sin(nx)\,dx$

Split:

$b_n=\frac{1}{\pi}\left(\int_{-\pi}^{\pi} x\sin(nx)\,dx+\pi\int_{-\pi}^{\pi}\sin(nx)\,dx\right)$

The second integral is zero:

$\int_{-\pi}^{\pi} \sin(nx)\,dx = 0$

So we only need:

$\int_{-\pi}^{\pi} x\sin(nx)\,dx$

Since the integrand is even:

$=2\int_{0}^{\pi} x\sin(nx)\,dx$

Use integration by parts:

$\int x\sin(nx)\,dx= -\frac{x\cos(nx)}{n}+\frac{\sin(nx)}{n^2}$

Evaluate from 0 to π:

$\int_{0}^{\pi} x\sin(nx)\,dx= -\frac{\pi\cos(n\pi)}{n}= -\frac{\pi(-1)^n}{n}$

Thus,

$\int_{-\pi}^{\pi} x\sin(nx)\,dx= -\frac{2\pi(-1)^n}{n}$

So,

$b_n=\frac{1}{\pi}\left(-\frac{2\pi(-1)^n}{n}\right)=\frac{2(-1)^{n+1}}{n}$

Final Fourier Series

$f(x)=\pi+\sum_{n=1}^{\infty}\frac{2(-1)^{n+1}}{n}\sin(nx)$
